{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/does_tree_contain_subtree.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}